const textAlign = {
  left: 'left',
  right: 'right',
  center: 'center'
}

const fontWeights = {
  bold: '700',
  medium: '600',
  regular: '400'
}

const fonts = {
  textAlign,
  fontWeights
}

export default fonts
